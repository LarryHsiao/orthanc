name: orthanc
description: "A Flutter desktop app that spawns a Claude Code session in an embedded terminal."
# The following line prevents the package from being accidentally published to
# pub.dev using `flutter pub publish`. This is preferred for private packages.
publish_to: 'none' # Remove this line if you wish to publish to pub.dev

# The following defines the version and build number for your application.
# A version number is three numbers separated by dots, like 1.2.43
# followed by an optional build number separated by a +.
# Both the version and the builder number may be overridden in flutter
# build by specifying --build-name and --build-number, respectively.
# In Android, build-name is used as versionName while build-number used as versionCode.
# Read more about Android versioning at https://developer.android.com/studio/publish/versioning
# In iOS, build-name is used as CFBundleShortVersionString while build-number is used as CFBundleVersion.
# Read more about iOS versioning at
# https://developer.apple.com/library/archive/documentation/General/Reference/InfoPlistKeyReference/Articles/CoreFoundationKeys.html
# In Windows, build-name is used as the major, minor, and patch parts
# of the product and file versions while build-number is used as the build suffix.
version: 1.1.11+15

environment:
  sdk: ^3.10.7

# Dependencies specify other packages that your package needs in order to work.
# To automatically upgrade your package dependencies to the latest versions
# consider running `flutter pub upgrade --major-versions`. Alternatively,
# dependencies can be manually updated by changing the version numbers below to
# the latest version available on pub.dev. To see which dependencies have newer
# versions available, run `flutter pub outdated`.
dependencies:
  flutter:
    sdk: flutter

  # The following adds the Cupertino Icons font to your application.
  # Use with the CupertinoIcons class for iOS style icons.
  cupertino_icons: ^1.0.8
  path_provider: ^2.1.4
  path: ^1.9.0
  flutter_pty: ^0.4.2
  xterm: ^4.0.0
  auto_updater: ^1.0.0
  package_info_plus: ^10.2.1
  shared_preferences: ^2.5.5
  url_launcher: ^6.3.2
  clock: ^1.1.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  fake_async: ^1.3.3

  # The "flutter_lints" package below contains a set of recommended lints to
  # encourage good coding practices. The lint set provided by the package is
  # activated in the `analysis_options.yaml` file located at the root of your
  # package. See that file for information about deactivating specific lint
  # rules and activating additional ones.
  flutter_lints: ^6.0.0

# For information on the generic Dart part of this file, see the
# following page: https://dart.dev/tools/pub/pubspec

# The following section is specific to Flutter packages.
flutter:

  # The following line ensures that the Material Icons font is
  # included with your application, so that you can use the icons in
  # the material Icons class.
  uses-material-design: true

  # To add assets to your application, add an assets section, like this:
  # assets:
  #   - images/a_dot_burr.jpeg
  #   - images/a_dot_ham.jpeg

  # An image asset can refer to one or more resolution-specific "variants", see
  # https://flutter.dev/to/resolution-aware-images

  # For details regarding adding assets from package dependencies, see
  # https://flutter.dev/to/asset-from-package

  # To add custom fonts to your application, add a fonts section here,
  # in this "flutter" section. Each entry in this list should have a
  # "family" key with the font family name, and a "fonts" key with a
  # list giving the asset and other descriptors for the font. For
  # example:
  # fonts:
  #   - family: Schyler
  #     fonts:
  #       - asset: fonts/Schyler-Regular.ttf
  #       - asset: fonts/Schyler-Italic.ttf
  #         style: italic
  #   - family: Trajan Pro
  #     fonts:
  #       - asset: fonts/TrajanPro.ttf
  #       - asset: fonts/TrajanPro_Bold.ttf
  #         weight: 700
  #
  # For details regarding fonts from package dependencies,
  # see https://flutter.dev/to/font-from-package

# Two independent xterm.dart 4.0.0 defects, both fixed on the `orthanc-integration`
# branch of the fork below, each also proposed upstream on its own branch:
#
# 1. The escape parser misreads xterm's `CSI > Ps ; Ps m` (modifyOtherKeys, a
#    keyboard-mode control) as plain SGR, spuriously setting underline/faint for
#    the entire session.
#    Fork branch: orthanc-sgr-private-marker-fix
#    Upstream PR: https://github.com/TerminalStudio/xterm.dart/pull/230
#
# 2. `CustomTextEdit` builds its `TextInputConfiguration` without a `viewId`, which
#    Flutter's Windows embedder rejects ("Could not set client, view ID is null").
#    The text input connection never attaches, and since `CustomTextEdit` has no
#    hardware-key fallback for printable characters, typing is dead on Windows —
#    while Enter and arrows still work, because those route through the Focus
#    onKeyEvent path. macOS tolerates the omission via its implicit view, which is
#    why this stayed hidden until Milestone 0's Windows pass.
#    Fork branch: orthanc-text-input-view-id
#    Upstream PR: https://github.com/TerminalStudio/xterm.dart/pull/231
#
# 3. OSC 8 (terminal hyperlinks) was unhandled entirely — the escape parser
#    only special-cased OSC 0/1/2 (title/icon); everything else, OSC 8
#    included, fell through to unknownOSC() and was dropped silently.
#    Adds Terminal.hyperlinkAt(CellOffset) -> String? and a dedicated
#    CellFlags.hyperlink underline bit. See
#    docs/superpowers/specs/2026-07-24-orthanc-osc8-hyperlinks-design.md.
#    Also fixes TerminalView.onTapUp being dead: TerminalGestureDetector's
#    TapGestureRecognizer only ever calls onSingleTapUp on a real tap-up,
#    but TerminalView never wired onSingleTapUp through, so a click never
#    reached PaneView's hyperlink-open callback.
#    Fork branch: orthanc-osc8-hyperlinks
#    Upstream PR: not filed
#
# Override until a real release includes both.
#
# flutter_pty 0.4.2's Windows backend (build_command(), flutter_pty_win.c)
# joins the executable and arguments into one Win32 command-line string with
# no quoting, so any executable under a space-containing path — e.g. Git for
# Windows' default install location, "C:\Program Files\Git\bin\bash.exe" —
# breaks: the child's own argv-recovery misparses the space as a token
# boundary. It also writes the executable twice (options->arguments[0]
# already duplicates it, for Unix's execvp() convention, but build_command()
# unconditionally wrote its own executable parameter first too).
# Fork branch: orthanc-windows-command-quoting
# Upstream PR: not filed
#
# Override until a real release includes this fix.
dependency_overrides:
  xterm:
    git:
      url: https://github.com/LarryHsiao/xterm.dart.git
      ref: 4eda00d
  flutter_pty:
    git:
      url: https://github.com/LarryHsiao/flutter_pty.git
      ref: c7c9deb
